# Issue Triage Eval Case

## case-id: incomplete-bug-report

目标能力：

把一个信息不完整的 bug report 分类，并输出可执行的追问。

输入：

```text
我点发布以后页面一直转圈。
```

期望行为：

- 标记为 `needs-info` 或同类状态。
- 追问版本、环境、复现步骤、预期行为、错误截图或日志。
- 不要直接假设根因。

失败判定：

- 直接给修复方案但没有追问。
- 直接归类为某个具体模块 bug，但没有依据。
- 没有说明还缺什么信息。
